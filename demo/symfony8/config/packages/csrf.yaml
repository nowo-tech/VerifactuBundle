# Enable CSRF protection for session-authenticated mutations when needed.
framework:
    csrf_protection:
        stateless_token_ids:
            - submit
            - authenticate
            - logout
