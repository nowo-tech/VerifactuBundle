{
	skip_install_trust
}

:80 {
	root * /app/public
	encode zstd br gzip
	# No worker: one PHP process per request (reliable for demos/CI; Symfony index.php works as-is)
	php_server {
		try_files {path} /index.php
	}
}
